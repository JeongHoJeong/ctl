{
  "name": "ctl-project",
  "version": "0.1.0",
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/** tests/** --no-error-on-unmatched-pattern --ext .ts",
    "test": "jest"
  }
}
